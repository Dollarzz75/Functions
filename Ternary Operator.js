// 26. Check if a number is positive or negative
let num = -5;
let result = num >= 0 ? "Positive" : "Negative";
console.log(result); // Negative


// 27. Set a default value for a variable
let username = "";
let displayName = username ? username : "Guest";
console.log(displayName); // Guest


// 28. Determine if a number is odd or even
let n = 7;
let oddOrEven = (n % 2 === 0) ? "Even" : "Odd";
console.log(oddOrEven); // Odd


// 29. Select between two colors
let isDarkMode = true;
let color = isDarkMode ? "Black" : "White";
console.log(color); // Black


// 30. Display "Adult
